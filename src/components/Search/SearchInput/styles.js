import styled from 'styled-components';

import { Container as Label } from '../../Form/Label/styles';
import { Input } from '../../Form/Input/styles';

export const SearchLabel = styled(Label)``;

export const SearchInput = styled(Input)``;
